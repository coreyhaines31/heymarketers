<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
  <div class="mb-8 text-center">
    <h1 class="text-3xl font-semibold" style="color: var(--color-foreground);">Explore Marketing Skills</h1>
    <p class="mt-2 text-sm" style="color: var(--color-muted-foreground);">
      Browse the core disciplines we track for marketer profiles. Pick a skill to dive deeper once directories are live.
    </p>
  </div>

  <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
    <% @skills.each do |skill| %>
      <div class="card hover:shadow-lg transition-shadow" data-skill="<%= skill.slug %>">
        <div class="card-body flex items-center justify-between">
          <div>
            <p class="text-lg font-medium" style="color: var(--color-foreground);"><%= skill.name %></p>
            <p class="text-sm mt-1" style="color: var(--color-muted-foreground);">
              Slug: <code class="text-xs"><%= skill.slug %></code>
            </p>
          </div>
          <span class="badge badge-outline">Directory soon</span>
        </div>
      </div>
    <% end %>
  </div>
</div>
