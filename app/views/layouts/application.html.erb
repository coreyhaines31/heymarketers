<!DOCTYPE html>
<html>
  <head>
    <title>HeyMarketers - Find Your Perfect Marketing Expert</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Connect with top marketing professionals and find the perfect match for your business needs.">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&family=IBM+Plex+Mono:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,100;1,200;1,300;1,400;1,500;1,600;1,700&family=Lora:ital,wght@0,400..700;1,400..700&display=swap" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen" style="background-color: var(--color-background); color: var(--color-foreground); font-family: var(--font-sans);">
    <!-- Header -->
    <header class="navbar">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <!-- Logo -->
          <div class="flex items-center">
            <%= link_to root_path, class: "text-xl font-bold transition-colors", style: "color: var(--color-foreground);" do %>
              HeyMarketers
            <% end %>
          </div>

          <!-- Desktop Navigation -->
          <div class="hidden lg:flex items-center space-x-1">
            <%= link_to "Find Marketers", marketers_path, class: "px-3 py-2 text-sm font-medium transition-colors hover:bg-opacity-80", style: "color: var(--color-muted-foreground); border-radius: var(--radius);" %>
            <%= link_to "Post a Job", "#", class: "px-3 py-2 text-sm font-medium transition-colors hover:bg-opacity-80", style: "color: var(--color-muted-foreground); border-radius: var(--radius);" %>
            <%= link_to "How It Works", "#", class: "px-3 py-2 text-sm font-medium transition-colors hover:bg-opacity-80", style: "color: var(--color-muted-foreground); border-radius: var(--radius);" %>
          </div>

          <!-- Desktop Auth Buttons -->
          <div class="hidden lg:flex items-center space-x-3">
            <% if user_signed_in? %>
              <span class="text-sm" style="color: var(--color-muted-foreground);">Hello, <%= current_user.full_name %></span>
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-ghost" %>
            <% else %>
              <%= link_to "Sign In", new_user_session_path, class: "btn btn-ghost" %>
              <%= link_to "Get Started", new_user_registration_path, class: "btn btn-primary" %>
            <% end %>
          </div>

          <!-- Mobile menu button -->
          <div class="lg:hidden">
            <button type="button" class="p-2 transition-colors" style="color: var(--color-muted-foreground); border-radius: var(--radius);" id="mobile-menu-button">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
              </svg>
            </button>
          </div>
        </div>

        <!-- Mobile Navigation Menu -->
        <div class="lg:hidden hidden" id="mobile-menu">
          <div class="px-2 pt-2 pb-3 space-y-1 border-t" style="background-color: var(--color-card); border-color: var(--color-border);">
            <%= link_to "Find Marketers", marketers_path, class: "block px-3 py-2 text-base font-medium transition-colors", style: "color: var(--color-muted-foreground); border-radius: var(--radius);" %>
            <%= link_to "Post a Job", "#", class: "block px-3 py-2 text-base font-medium transition-colors", style: "color: var(--color-muted-foreground); border-radius: var(--radius);" %>
            <%= link_to "How It Works", "#", class: "block px-3 py-2 text-base font-medium transition-colors", style: "color: var(--color-muted-foreground); border-radius: var(--radius);" %>
            <% if user_signed_in? %>
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "block px-3 py-2 text-base font-medium transition-colors", style: "color: var(--color-muted-foreground); border-radius: var(--radius);" %>
            <% else %>
              <%= link_to "Sign In", new_user_session_path, class: "block px-3 py-2 text-base font-medium transition-colors", style: "color: var(--color-muted-foreground); border-radius: var(--radius);" %>
              <%= link_to "Get Started", new_user_registration_path, class: "btn btn-primary" %>
            <% end %>
          </div>
        </div>
      </div>
    </header>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="alert alert-success mx-4 mt-4 max-w-4xl mx-auto flex items-center">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 shrink-0 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span><%= notice %></span>
      </div>
    <% end %>

    <% if alert %>
      <div class="alert alert-error mx-4 mt-4 max-w-4xl mx-auto flex items-center">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 shrink-0 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span><%= alert %></span>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="flex-1">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-100 border-t border-gray-200 mt-16">
      <div class="max-w-7xl mx-auto py-12 px-4">
        <div class="text-center">
          <p class="text-lg font-bold text-gray-900">HeyMarketers</p>
          <p class="text-gray-600 mt-2">Connecting businesses with marketing talent since 2024</p>
          <div class="flex justify-center space-x-6 mt-6">
            <a href="#" class="text-gray-500 hover:text-indigo-600">Privacy Policy</a>
            <a href="#" class="text-gray-500 hover:text-indigo-600">Terms of Service</a>
            <a href="#" class="text-gray-500 hover:text-indigo-600">Contact</a>
          </div>
        </div>
      </div>
    </footer>

    <script>
      // Mobile menu toggle
      document.getElementById('mobile-menu-button').addEventListener('click', function() {
        const menu = document.getElementById('mobile-menu');
        menu.classList.toggle('hidden');
      });
    </script>
  </body>
</html>
